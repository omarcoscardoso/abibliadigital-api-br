version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: abibliadigital-api
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_PATH=/app/biblia.db
    restart: unless-stopped
